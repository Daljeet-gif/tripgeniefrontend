# Backend API base URL (must end with /api)
VITE_API_URL=http://localhost:5000/api

# Optional: Geoapify key for nicer dark map tiles.
# If left empty, the interactive map falls back to free OpenStreetMap tiles.
VITE_GEOAPIFY_KEY=
